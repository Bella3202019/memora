# Core memory extraction system dependencies
python-dotenv
neo4j
openai
pyyaml

# Optional LLM backends (base install works without these):
#   pip install anthropic      -> llm.backend: anthropic
#   pip install google-genai   -> llm.backend: gemini
