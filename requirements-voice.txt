# Voice agent dependencies (LiveKit)
# These are separate from the core memory extraction system.
# Install with: pip install -r requirements-voice.txt
# Also install Memora itself (pip install -e .) — the voice agent's memory
# comes from Memora's own MCP server (`memora mcp`), not a third-party service.

livekit
livekit-agents
livekit-plugins-silero
livekit-plugins-deepgram
livekit-plugins-openai
livekit-plugins-turn-detector
livekit-plugins-noise-cancellation
